import { useEffect } from "react";
import { useSearchParams } from "next/navigation";

export default function SearchParamsHandler() {
  // This component was previously used to sync URL params to Redux state.
  // With the migration to React Query, this logic is handled directly in the data fetching hooks.
  // Keeping this file as a placeholder to avoid breaking imports in parent components.
  return null;
}
